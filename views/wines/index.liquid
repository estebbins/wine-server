{% layout 'layout.liquid' %}
{% block content %}
    <!-- we need to account for two conditions here -->
    <!-- first, if we have wines in our array -->
    <!-- second, if there are no wines in the array -->
    {% if wines.length > 0 %}
        <h2>All Wines</h2>
        <ul>
            <!-- now we can loop over our array of wines -->
            <!-- and produce some html for every wine that we have -->
            {% for wine in wines %}
                <li>{{ wine.brand }}</li>
            {% endfor %}
        </ul>
    {% else %}
        <h2>No wines yet... Go create some!</h2>
    {% endif %}
{% endblock %}